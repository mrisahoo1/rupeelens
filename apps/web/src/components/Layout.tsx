import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { motion } from 'framer-motion';
import { BarChart3, Upload, ReceiptText, Sparkles, WalletCards, RotateCcw, FileText, Landmark, Settings, ShieldCheck, PlugZap, LogOut } from 'lucide-react';
import { tokenStore, type User } from '../api';
import { Badge } from './ui';
const nav = [['Dashboard','/',BarChart3],['Upload Center','/upload',Upload],['Transactions','/transactions',ReceiptText],['Insights','/insights',Sparkles],['Budgets','/budgets',WalletCards],['Revisit Board','/revisit',RotateCcw],['Reports','/reports',FileText],['Accounts & Cards','/accounts',Landmark],['Rules','/rules',ShieldCheck],['BillPay & Integrations','/integrations',PlugZap],['Settings','/settings',Settings]] as const;
export default function Layout({user}:{user:User}) { const navigate = useNavigate(); return <div className="min-h-screen lg:flex"><aside className="sticky top-0 z-20 border-b border-white/10 bg-ink/75 p-4 backdrop-blur-xl lg:h-screen lg:w-72 lg:border-b-0 lg:border-r"><div className="mb-8 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-md bg-emeraldx text-xl font-black text-ink">₹</div><div><div className="font-display text-xl font-black">RupeeLens</div><div className="text-xs text-white/45">Fintech spend OS</div></div></div>{user.account_type==='demo' && <div className="mb-4 rounded-md border border-saffronx/25 bg-saffronx/10 p-3 text-xs text-saffronx">Demo mode — sample data only.</div>}<nav className="grid gap-1">{nav.map(([label,path,Icon])=><NavLink key={path} to={path} className={({isActive})=>`flex items-center gap-3 rounded-md px-3 py-2.5 text-sm transition ${isActive?'bg-white/12 text-emeraldx':'text-white/62 hover:bg-white/7 hover:text-white'}`}><Icon size={17}/>{label}</NavLink>)}</nav><button onClick={()=>{tokenStore.clear(); navigate('/login')}} className="mt-6 flex w-full items-center gap-3 rounded-md px-3 py-2 text-sm text-white/60 hover:bg-white/7"><LogOut size={17}/> Logout</button></aside><main className="relative flex-1 overflow-hidden"><div className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8"><motion.header initial={{opacity:0,y:-8}} animate={{opacity:1,y:0}} className="mb-6 flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm text-white/50">See where your money actually goes.</p><h1 className="font-display text-3xl font-black tracking-tight">Personal finance intelligence</h1></div><div className="flex items-center gap-2"><Badge tone={user.account_type==='demo'?'warn':'good'}>{user.username}</Badge><Badge>JWT protected</Badge></div></motion.header><Outlet /></div></main></div> }
