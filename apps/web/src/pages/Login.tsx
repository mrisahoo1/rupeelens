import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { motion } from 'framer-motion';
import { CreditCard, LockKeyhole, Sparkles } from 'lucide-react';
import { api, tokenStore } from '../api';
import { Button, Card, GhostButton, Input } from '../components/ui';
const fake = ['Food & Dining up 18%','3 subscriptions detected','₹12,430 on UPI this month','Credit card payment excluded from spend'];
export default function Login(){ const [username,setUsername]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const nav=useNavigate(); async function doLogin(u=username,p=password){ try{ const res=await api.login(u,p); tokenStore.set(res.access_token); nav('/'); } catch(e){ setError('Invalid username or password'); } } return <main className="grid min-h-screen lg:grid-cols-[1.1fr_.9fr]"><section className="relative flex min-h-[55vh] items-center overflow-hidden p-8 lg:p-16"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="max-w-3xl"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-emeraldx/20 bg-emeraldx/10 px-3 py-1 text-sm text-emeraldx"><Sparkles size={15}/> Premium Indian spend intelligence</div><h1 className="font-display text-6xl font-black leading-none sm:text-7xl">RupeeLens</h1><p className="mt-5 max-w-xl text-xl text-white/62">See where your money actually goes.</p><div className="mt-10 grid gap-3 sm:grid-cols-2">{fake.map((text,i)=><motion.div key={text} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:.15+i*.08}} className="glass card p-4"><div className="mb-3 flex h-9 w-9 items-center justify-center rounded-md bg-white/10"><CreditCard size={17}/></div><div className="font-bold">{text}</div><div className="mt-1 text-xs text-white/45">Live-style preview, sample only</div></motion.div>)}</div></motion.div></section><section className="flex items-center justify-center p-6"><Card className="w-full max-w-md p-7"><div className="mb-6"><div className="mb-3 grid h-12 w-12 place-items-center rounded-md bg-emeraldx text-ink"><LockKeyhole/></div><h2 className="text-2xl font-black">Enter your spend OS</h2><p className="mt-1 text-sm text-white/50">Main credentials come from environment variables.</p></div><div className="grid gap-3"><Input placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)}/><Input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>{if(e.key==='Enter') doLogin()}}/>{error && <p className="text-sm text-saffronx">{error}</p>}<Button onClick={()=>doLogin()}>Login securely</Button><GhostButton onClick={()=>doLogin('demo','demo123')}>Explore demo mode</GhostButton></div></Card></section></main> }
