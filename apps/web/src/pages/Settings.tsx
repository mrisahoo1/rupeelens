import { Card, Badge } from '../components/ui';
export default function Settings(){ return <div className="grid gap-4 md:grid-cols-2"><Card><Badge tone="good">secure by default</Badge><h3 className="mt-4 text-xl font-black">Environment-owned credentials</h3><p className="mt-2 text-sm leading-6 text-white/55">Main username and password/hash are loaded from environment variables. JWT secret, database URL, storage mode, and AI flags are also environment-driven.</p></Card><Card><Badge>storage</Badge><h3 className="mt-4 text-xl font-black">Private uploads</h3><p className="mt-2 text-sm leading-6 text-white/55">Local development stores uploads under a user partition. Production should connect private blob storage through the existing storage boundary.</p></Card><Card><Badge tone="warn">optional</Badge><h3 className="mt-4 text-xl font-black">AI insights disabled</h3><p className="mt-2 text-sm leading-6 text-white/55">The deterministic insight engine is active. OpenAI-compatible calls are future-ready and must be enabled explicitly with env flags.</p></Card></div> }
