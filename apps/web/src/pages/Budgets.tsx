import { useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { api } from '../api';
import { currentMonth, rupee } from '../lib';
import { Button, Card, Input, Select } from '../components/ui';
const categories=['Overall','Food & Dining','Groceries','Transport','Shopping','Subscriptions','Utilities','Travel','Fuel'];
export default function Budgets(){ const qc=useQueryClient(); const [month,setMonth]=useState(currentMonth()); const [category,setCategory]=useState('Overall'); const [amount,setAmount]=useState(''); const {data=[]}=useQuery({queryKey:['budgets'], queryFn:api.budgets}); const add=useMutation({mutationFn:()=>api.addBudget({month,category,amount:Number(amount)}), onSuccess:()=>{setAmount(''); qc.invalidateQueries({queryKey:['budgets']})}}); return <div className="grid gap-5"><Card><div className="grid gap-3 md:grid-cols-[180px_1fr_1fr_auto]"><Input type="month" value={month} onChange={e=>setMonth(e.target.value)}/><Select value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(c=><option key={c}>{c}</option>)}</Select><Input placeholder="Budget amount" value={amount} onChange={e=>setAmount(e.target.value)}/><Button onClick={()=>add.mutate()}>Save budget</Button></div></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.map((b:any)=><Card key={b.id}><div className="text-sm text-white/45">{b.month}</div><div className="mt-1 text-xl font-black">{b.category}</div><div className="mt-3 text-2xl text-emeraldx">{rupee(b.amount)}</div></Card>)}</div></div> }
